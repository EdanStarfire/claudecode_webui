# Backend configuration for development
# Default values point to test server on port 8001

# Backend host (default: 0.0.0.0 for all network interfaces)
VITE_BACKEND_HOST=0.0.0.0

# Backend port (default: 8001 for test server)
# Use 8000 for production instance
VITE_BACKEND_PORT=8001
